# Local development default. Set the deployed HTTPS backend URL in Vercel.
VITE_API_BASE_URL=http://localhost:8080

# Optional. Production uses the current Vercel origin and its /api/auth rewrite.
# Set this only when an environment has a dedicated same-origin auth proxy.
# VITE_AUTH_API_BASE_URL=https://app.example.com
